apiVersion: v1
kind: Service
metadata:
  labels:
    app: coreyapp-deploy
  name: coreyapp-deploy-lb-svc
spec:
  ports:
  - port: 4000
    protocol: TCP
    targetPort: 4000
  selector:
    app: coreyapp-deploy
  type: LoadBalancer
status:
  loadBalancer: {}
