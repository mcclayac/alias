apiVersion: v1
kind: Service
metadata:
  labels:
    app: tonyapp-deploy
  name: tonyapp-deploy-clusterip-service
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: tonyapp-deploy
  type: ClusterIP
status:
  loadBalancer: {}
