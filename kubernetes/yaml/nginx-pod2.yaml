apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-pod
    app: dev
  name: nginx-pod2
spec:
  containers:
  - image: nginx
    name: nginx2-container
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
